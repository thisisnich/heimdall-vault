---
id: "94252d00-3e4d-4bf5-8db0-3d206ebb96e8"
title: "S Ask Before Placing It"
type: vault_note
vault: wiki
created: 2026-05-17
updated: 2026-05-17
checksum: "sha256:8d1aed1c60673963"
source: heimdall/auto
---

# S Ask Before Placing It

- s, ask before placing it
- After all files are integrated, offer to commit and push to git
```

---

## QUICK VERSION

```
Files are in `_to_upload/`. Read `.vault-index.md`, run `python _tools/convert_to_md.py`, clean up each output in `_to_upload/converted/` (fill YAML frontmatter, add cross-links, fix formatting), move to the correct course folder, update the vault index and MOC, then summarise what was added.
```

---

## Supported Formats

| Format | Extensions |
|--------|-----------|
| PDF | `.pdf` |
| PowerPoint | `.pptx`, `.ppt` |
| Word | `.docx`, `.doc` |
| Excel | `.xlsx`, `.xls` |
| CSV | `.csv` |
| HTML | `.html`, `.htm` |
| Images | `.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.webp` |

> **Note on scanned PDFs:** Text extraction only works on text-based PDFs. For scanned/image PDFs, the output will be limited. For better results, use a PDF with selectable text, or take a photo and use the image conversion option. <!-- id:94252d00-3e4d-4bf5-8db0-3d206ebb96e8 ts:2026-05-17 07:49 -->
- s, ask before placing it
- After all files are integrated, offer to commit and push to git
```

---

## QUICK VERSION

```
Files are in `_to_upload/`. Read `.vault-index.md`, run `python _tools/convert_to_md.py`, clean up each output in `_to_upload/converted/` (fill YAML frontmatter, add cross-links, fix formatting), move to the correct course folder, update the vault index and MOC, then summarise what was added.
```

---

## Supported Formats

| Format | Extensions |
|--------|-----------|
| PDF | `.pdf` |
| PowerPoint | `.pptx`, `.ppt` |
| Word | `.docx`, `.doc` |
| Excel | `.xlsx`, `.xls` |
| CSV | `.csv` |
| HTML | `.html`, `.htm` |
| Images | `.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.webp` |

> **Note on scanned PDFs:** Text extraction only works on text-based PDFs. For scanned/image PDFs, the output will be limited. For better results, use a PDF with selectable text, or take a photo and use the image conversion option. <!-- id:94252d00-3e4d-4bf5-8db0-3d206ebb96e8 ts:2026-05-17 07:49 -->
